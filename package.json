{
  "name": "linkhub-android",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:android": "npx cap add android && npx cap copy android && cd android && chmod +x gradlew && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
